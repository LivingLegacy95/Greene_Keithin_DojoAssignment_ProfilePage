function hide(element) {
    element.remove();
}
// if the clickable element is the same as the the element you wish to effect, then use"this"
// if they're different then use the "id" of the element you wish to effect.

function changeName(element){
    element.innerText = "Tony The Tiger" 
}